# Setup

:::{rst-class} lead
Configure Patcher for your environment. The same credentials power both the CLI and the library; set them up once.
:::

Configure Patcher for your environment after install. The same credentials power both the `patcherctl` CLI and the `PatcherClient` library. Set them up once for whichever surface you use first, and the other picks them up automatically.

```{toctree}
:maxdepth: 1

cli
library
```
