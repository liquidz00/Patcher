---
description: "Autodoc reference for the five tools registered on the Patcher MCP server."
---

# MCP Tools

The five tools registered on the Patcher MCP server. Each tool's function signature defines the JSON schema clients see; the docstring is what the client's LLM reads to decide when to call it.

For end-user setup snippets see {doc}`/getting-started/mcp`. For prompt examples that exercise these tools, see {doc}`/guides/mcp-recipes`.

```{eval-rst}
.. automodule:: patcher_api.mcp.tools
   :members:
```
