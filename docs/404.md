---
orphan: true
description: "Page not found. The URL you tried isn't in the Patcher docs catalog. Head back to the docs home or use the search box."
---

# 404 — Patch Not Found

:::{rst-class} lead
Looks like this page rolled out to 0% of hosts.
:::

The URL you tried isn't in the catalog. A few possible causes, ranked by likelihood:

- **The page moved.** The docs got reorganized recently. Try the [docs home](https://docs.patcherctl.dev/en/latest/) or use the search box.
- **You typed it wrong.** Happens. Refresh and double-check.
- **You're looking at a stale link from somewhere else.** If you got here from another page on this site, [open an issue](https://github.com/liquidz00/Patcher/issues/new) so we can fix the link.
- **You're a search engine crawler.** Sorry about the wasted request. Try [sitemap.xml](https://docs.patcherctl.dev/sitemap.xml).

```{tip}
If you were looking for command-line help, the {doc}`Usage pages </guides/export>` document every flag inline. If you were looking for the API, the [endpoints page](https://docs.patcherctl.dev/en/latest/reference/api/endpoints.html) covers what's public.
```

Otherwise, head back to the [Patcher docs home](https://docs.patcherctl.dev/en/latest/) and try again.
